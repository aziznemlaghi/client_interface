import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'client_front_2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
